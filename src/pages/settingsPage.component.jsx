import PageContainer from "../components/pageContainer/pageContainer.component";

const SettingsPage = ({ onClick }) => {
  return <PageContainer onClick={onClick}></PageContainer>;
};

export default SettingsPage;
